describe('gen-otp', () => {
  it('dummy test', () => {
    // This is a dummy test to ensure the test suite runs.
    expect(true).toBe(true);
  });
  it('dummy test 2', () => {
    // This is another dummy test to ensure the test suite runs.
    expect(true).toBe(false);
  });
});
